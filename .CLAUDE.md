# 轻舟 — 沉浸式学习专注器（Flutter 版）

## 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 框架 | Flutter (Dart) | 跨平台原生渲染，保证计时与动画的流畅性 |
| 状态管理 | flutter_riverpod | 轻量、响应式，专注/休息状态流管理 |
| 本地持久化 | hive + shared_preferences | hive 存任务/历史/统计；shared_preferences 存用户设置 |
| 图表 | fl_chart | 柱状图、环形图、热力图 |
| 本地通知 | flutter_local_notifications | 计时结束推送 |
| 震动反馈 | vibration | 开始/完成/严格模式切换时轻震 |
| 动画 | 内置 AnimationController + Tween | 圆环进度、数字滚动、底部弹窗阻尼 |
| 路由 | go_router | 声明式路由，页面切换从右滑入 |
| 图标 | lucide_icons / phosphor_flutter | 1.5px 线性图标风格 |

---

## 第一部分：功能设计（PRD 视角）

### 0. 视觉与交互风格定义（设计总纲）

作为一款面向年轻考研党的工具，视觉风格必须区别于传统效率软件的“硬核、粗糙、堆砌功能”，要给用户一种“这是一个有品味的人在做考研这件苦差事”的心理暗示。

**整体设计语言：极致留白与轻量卡片化**
- 留白：页面四周及元素间距必须充裕，杜绝信息拥挤。用户打开 APP 的直觉应该是“干净、透气”，而不是“任务好多我好焦虑”。
- 卡片化：所有功能区块（待办、统计图表、设置项组）均采用微圆角（12px–16px）卡片承载，卡片之间不贴边，悬浮于浅灰底色之上，营造层次感。

**色彩体系：低饱和度“莫兰迪/高级灰”调性**
- 主背景：日间模式为纯白（`#FFFFFF`）或极浅燕麦灰（`#FAFAFA`）；夜间模式为深空灰（`#121212`），绝对不使用纯黑。
- 主色调：不使用高饱和度的红黄蓝。推荐使用“静谧蓝”或“知性绿”作为全局强调色（用于按钮高亮、开关打开状态等），传递专注、平静的情绪。
- 学科专属色（核心特色）：这是贯穿全局的色彩密码，必须精心调配，避免刺眼：
  - 政治：绛红 / 砖红（而非大红）
  - 英语：雾霾蓝 / 灰蓝（而非亮蓝）
  - 数学：豆绿 / 鼠尾草绿（而非翠绿）
  - 专业课：灰紫 / 薰衣草紫（而非亮紫）
  - 其他：暖灰

**文字层级**：主文字（任务名、大数字）使用高对比度深灰（接近黑但不纯黑，`#1A1A1A`）；辅助文字（时间戳、提示语）使用中灰（`#8E8E93`），拉开视觉层级。

**字体与排版：数字至上**
- 时间数字：倒计时和统计页面的大数字必须采用“细体/轻字重”（如 `FontWeight.w300`），字号极大。细体大字能显著提升界面的“高级感”和“精致感”，区别于传统时钟的笨重。
- 任务文字：采用常规字重，字间距微调宽一点（0.5px），提升阅读舒适度。

**图标与图形：线性与圆润**
- 全局图标一律采用 1.5px 或 2px 线性图标，线条端点圆润。
- 拒绝拟物化设计（不画真实的番茄、不画真实的书本），用最抽象的几何图形表达含义。

**动效原则：克制与物理感**
- 不花哨：拒绝弹跳、旋转、霓虹闪烁等过度活泼的动效，考研场景需要“沉稳”。
- 物理阻尼：底部弹窗的升起、圆环进度的推进，应模拟轻微的物理阻尼感（类似拉动一根韧性很好的皮筋），过渡曲线使用 `Curves.easeInOutCubic`。
- 状态切换：数字的变化不要生硬跳变，要有平滑的滚动或缩放过渡。

---

### 1. 专注计时模块

**Flutter 实现要点**：
- 中央圆环使用 `CustomPainter` 绘制，搭配 `AnimationController` 驱动进度与颜色渐变。
- 倒计时数字使用 `AnimatedBuilder` 或 `TweenAnimationBuilder` 实现丝滑过渡，避免 `setState` 高频刷新整块 UI。
- 底部任务选择弹窗使用 `showModalBottomSheet` + `AnimationController` 自定义进入曲线（`easeInOutCubic`）。
- 计时核心逻辑抽离为 `StateNotifier`（riverpod），在后台通过 `WidgetsBindingObserver` 处理应用生命周期，切后台时仍保持计时。
- 计时结束通过 `flutter_local_notifications` 发送通知，同时调用 `vibration` 轻震。

**界面呈现**：
主界面正中央是一个巨大的动态圆环（或极简数字时钟），默认显示“25:00”。
圆环上方提供一排横向滑动的“快捷时长胶囊”，胶囊背景为浅灰底黑字，选中态使用“主色调”填充。

**操作流程**：
- **开始**：用户点击中央圆环，屏幕底部弹起半屏弹窗（带轻微阻尼动效）。弹窗上半部分显示“今日待办列表”（每个待办左侧有一个小圆点标识“学科专属色”），下半部分提供“自由专注”按钮。用户点击某个待办后，弹窗收起，圆环立刻开始倒计时，圆环颜色瞬间渐变为该任务的学科色。
- **进行中**：界面极致沉浸，仅保留中央倒计时数字、顶部当前任务名称（如“刷真题100道 · 数学”，数学两字为灰蓝色），底部设一个极不显眼的“放弃”文字按钮。
- **完成**：倒计时归零时，手机短震动，数字变为打勾动画，背景从白色渐变为极浅的学科色（如极浅的灰蓝），弹出轻量级完成卡片提示“专注了 25 分钟”，随后自动进入休息倒计时（整个界面加上一层半透明深色遮罩，模拟休息氛围）。

**业务规则（严格模式）**：
- 默认关闭：点击“放弃”直接退出。
- 开启后：点击“放弃”会弹出二次确认卡片，卡片顶部有一道细细的“警示红”线条，强行打破界面的宁静，引起注意。

---

### 2. 任务与规划模块（极简版）

**Flutter 实现要点**：
- 列表使用 `ListView.builder`，任务卡片为 `Dismissible`，向左滑动露出红色删除背景。
- 添加任务底部面板使用 `showModalBottomSheet`，内部包含 `TextField` + 横向 `ListView`（学科胶囊选择）。
- 学科胶囊使用 `ChoiceChip` 或自定义 `Container` 实现，颜色绑定学科专属色常量表。
- 数据模型使用 `Hive` 对象（`@HiveType` 注解），任务仅限当日有效，每天 0 点自动清空或归档。

**界面呈现**：
位于主界面上半部分（圆环上方），以垂直滚动的极简列表展示，列表项自带浅灰背景卡片。
空状态文案：“今天还没有计划，点击 + 添加”（使用前述的“中灰”辅助色，居中显示）。

**操作流程**：
- **添加（两步闭环）**：点击列表右侧“+”号（线性图标），底部弹出输入面板。第一行输入任务名；第二行横向滑动选择学科标签（标签为带背景的胶囊，颜色对应学科专属色）。点击“确定”入库。
- **完成**：任务通过“专注计时”走完后，文字自动加上中划线（颜色变浅灰），卡片透明度略微降低，沉底排列。
- **删除**：向左轻滑任务卡片，底层露出纯红色的“删除”背景块。

**业务规则**：
任务仅限“当日有效”，不提供跨天延期功能。

---

### 3. 统计复盘模块

**Flutter 实现要点**：
- 顶部维度切换 Tab 使用 `TabBar` 或自定义 `Row` + `AnimatedContainer` 指示器。
- 核心数据卡片使用 `Row` 并排两个 `Card`，内部数字使用 `Text` + `fontWeight: FontWeight.w300`。
- 图表统一使用 `fl_chart`：
  - 日视图：`BarChart`（圆角矩形柱子）。
  - 周视图：`BarChart` + `PieChart`（环形图，中心镂空显示总时长）。
  - 月视图：自定义 `GridView` 热力图，色块深浅映射专注时长。
- 图表颜色严格使用学科专属色常量，避免高饱和彩虹色。

**界面呈现**：
顶部为维度切换 Tab：“日 / 周 / 月”，当前选中项底部有一条短横线指示器（主色调）。
中间为两个大字体的核心数据卡片并排（浅灰背景卡片）：“专注 X 小时 X 分” 和 “完成 X 个番茄”。
下方为图表区域，图表内部不带复杂的网格线，保持清爽。

**图表规则**：
- 日视图：展示当日各时段的专注时长柱状图（柱子为圆角矩形）。
- 周视图：展示周一到周日的每日专注时长柱状图；下方附带一个“学科占比”环形图（中心镂空显示总时长），环形图的各个扇区严格对应政治红、英语蓝、数学绿等。
- 月视图：展示日历热力图，颜色深浅代表当天专注时长，色块颜色默认使用“主色调”的深浅变化，如果当天只学了一门，可显示该“学科色”。

底部明细：时间倒序的流水列表，字号偏小，主要信息加粗，次要时间信息用中灰色。

---

### 4. 设置模块

**Flutter 实现要点**：
- 页面为 `Scaffold` + `ListView`，设置项使用 `ListTile` 分组展示。
- `Switch` 使用 `CupertinoSwitch` 或自定义圆润 Toggle，打开时颜色变为主色调。
- 时长调整使用 `showModalBottomSheet` 内嵌 `NumberPicker` 或自定义加减按钮，保持极简。
- 权限状态检测使用 `permission_handler`（通知权限）和 `vibration` 的可用性检查。

**界面呈现**：单页面，无底部 Tab。

**功能分组**：
- **计时设置组**：默认专注/休息时长（右侧显示数字，点击出现极简的加减按钮）、长休息间隔。
- **自律设置组**：严格模式开关（使用 iOS 风格的圆润 Toggle，打开时颜色变为主色调），下方有一行小字提示。
- **权限与隐私组**：通知权限、震动权限。右侧状态如果为“未开启”，显示为警示性的浅橙色文字“去开启”；如果已开启，显示为浅灰色“已开启”。

---

### 5. 全局交互规范

**Flutter 实现要点**：
- **零引导**：首次安装无滑动引导页，`main.dart` 直接进入 `FocusView`，用 `SharedPreferences` 记录是否首次打开即可。
- **单手操作**：所有核心交互按钮（开始、添加、确定）必须落在屏幕下半部；底部弹窗高度不超过屏幕 60%。
- **过渡动画**：页面切换使用 `go_router` 的自定义转场（从右滑入，`SlideTransition`）；底部弹窗使用带轻微阻尼的弹性动画；倒计时数字变化使用 `TweenAnimationBuilder` 实现滚动或缩放过渡，绝不能生硬闪烁。
- **触觉反馈**：仅在“开始专注”、“完成专注”、“开启/关闭严格模式”这三个关键节点调用 `vibration.vibrate(duration: 50)`，其他普通点击一律静音，避免打扰。
